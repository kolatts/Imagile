name: Publish Framework NuGet Package
on:
    push:
        branches:
        - main
        paths: 
        - 'Imagile.Framework/**'
jobs:
    call-workflow-passing-data:
        uses: nuget.yml@main
        with:
            project-name: 'Imagile.Framework'
